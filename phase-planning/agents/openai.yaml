interface:
  display_name: "Phase Planning"
  short_description: "Plan phases with a task ledger and per-task specs."
  default_prompt: "Plan the named implementation phase using a task ledger and one detail file per task."
